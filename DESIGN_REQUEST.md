# [디자인 요청서] 공유 타이머 — 진행자(컨트롤러) 화면 정리

> 사용법: 아래 전체를 복사해 디자인 도구/세션에 붙여넣고, **현재 화면 스크린샷을 함께 첨부**하세요.

---

## 무엇을 원하나
공유 카운트다운 타이머의 **진행자(컨트롤러) 화면** UI를 정리하고 싶다.
기능은 그대로 두고 **시각적 위계와 그룹 정리**만 개선한다. (HTML/CSS로 구현)

## 사용 맥락
- 여러 사람이 각자 기기로 같은 카운트다운을 실시간으로 본다.
- **뷰어 화면**(청중): 큰 숫자만 심플하게 본다. ← 지금 이대로 유지.
- **컨트롤러 화면**(진행자): 아래 기능이 다 모여 있어 복잡하다. ← 이 화면을 정리.
- 사용자층에 **고령층·IT 비전문가**가 포함된다 → 버튼·글씨 크게, 직관적으로.
- 활용 예: 발표/회의 진행, 온라인 게임 공격대(레이드) 지휘.

## 현재 화면의 문제
1. 색이 너무 많다(초록·주황·회색·파랑·보라) → 산만하다.
2. 기능 그룹 구분이 약해 다 비슷한 비중으로 나열돼 있다.
3. 시각적 위계가 없어 "무엇이 중요한지" 안 보인다.
4. 대기 상태 표시가 "대기 중"뿐 → 언제 시작하는지(시각) 안 보인다.

## 화면에 반드시 들어가야 하는 기능(하나도 빼지 말 것)
- **대형 타이머 숫자** (예: 00:34) — 가장 크게, 화면 중심.
- **상태 표시** — 대기 중 / 진행 중 / 일시정지 / 시간 종료 / 예약 대기 중.
- **접속자 수**(구석), **역할 배지**(진행자/뷰어, 구석).
- **① 재생 컨트롤**: 시작 · 일시정지 · 리셋
- **② 시간 미세조정**: −1분 · −10초 · +10초 · +1분
- **③ 예약 시작**: 시각 입력(시:분) + [이 시각에 시작] + [예약 취소]
- **④ 즉석 메시지**: 텍스트 입력 + [메시지 표시] + [지우기]  (전원 화면에 큰 글씨로 방송)
- **⑤ 예약 메시지**: 분·초 입력 + 메시지 입력 + [예약 추가] + 등록된 목록(각 항목 삭제)
- **화면 위쪽 메시지 배너** — 방송된 메시지가 타이머 위에 크게 표시됨.
- **하단**: 전체화면 버튼, 소리 켜기 버튼.

## 개선 목표(이렇게 바꿔줘)
- **색 절제**: 강조색 1개(시작 = 초록 계열)만 쓰고 나머지 버튼은 중립 톤(회색 계열)으로 통일. 무지개색 지양.
- **기능별 그룹화**: 위 ①~⑤를 각각 **카드 또는 구분선**으로 묶고, 그룹마다 **작은 제목**을 단다.
- **우선순위 레이아웃**: 자주 쓰는 재생 컨트롤은 위/눈에 잘 띄게. 가끔 쓰는 ③예약시작·⑤예약메시지는 **접기(아코디언)**로 숨겨 첫 화면을 단순하게.
- **대기 상태 개선**: "대기 중" 대신 **"오후 9:00 시작 · 시작까지 00:34"** 처럼 정보를 명확히.
- 전체적으로 여백을 넉넉히, 정렬을 맞춰 정돈된 느낌.

## 제약(반드시 지킬 것)
- **다크 배경 유지**, 타이머 숫자는 지금처럼 크게.
- 남은 시간이 적어지면 배경색이 바뀌는 경고 표시(경고=주황, 위험=빨강, 종료=진한 빨강)는 유지.
- 버튼 터치 영역·글씨를 **고령층도 쉽게** 누르도록 충분히 크게.
- **뷰어(청중) 화면은 숫자만** 심플하게 유지(컨트롤 없음).
- 순수 **HTML/CSS**로 구현(별도 프레임워크 없이, 기존 Flask 앱에 바로 넣을 수 있게).
- 반응형: 노트북·폰 모두에서 깨지지 않게.

## 참고 — 현재 색상
- 배경(기본): 진한 네이비 `#0f172a`
- 경고/위험 배경: 주황 `#78350f`, 빨강 `#7f1d1d`
- 버튼: 시작 `#22c55e`(초록) · 일시정지 `#f59e0b`(주황) · 리셋 `#64748b`(회색) · 메시지 `#8b5cf6`(보라) · 예약 `#0ea5e9`(파랑)
  → 이 제각각인 색을 **강조 1색 + 중립**으로 정리해달라.

## 산출물
- 정리된 컨트롤러 화면의 **HTML + CSS** (한 벌).
- 가능하면 뷰어 화면과의 공통 스타일도 함께.
