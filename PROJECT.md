# 프로젝트: 공유 타이머 (같은 공간용 MVP)

## 작업자 정보
- 나는 비개발자 기획자다. Python 초보 수준으로 설명할 것.
- 프로토타입을 만들어 개발팀에 전달한다. 실서버 직접 배포는 안 한다.

## 환경 주의 (중요)
- 이 PC에는 Python이 여러 개 있다(3.13, 3.12, anaconda, 3.11).
  지난번 패키지 설치가 꼬인 적 있다.
- 반드시 가상환경(venv)을 만들어 진행하고,
  어떤 Python을 쓰는지 / 패키지가 어디 설치되는지 명확히 알려줄 것.

## 만들 것
같은 와이파이에서 여러 명이 각자 노트북·폰으로 동일한 카운트다운을
실시간으로 보는 공유 타이머 (Flask + Flask-SocketIO).

## 코드 작성 시 필수 보안 원칙
- 뷰어 링크와 컨트롤러 링크를 분리한다.
- 컨트롤러 토큰은 반드시 서버에서 검증한다(클라이언트 입력 불신).
- 외부로 나가는 데이터에 control_token을 절대 포함하지 않는다.
- 다중 사용자 환경이므로 데이터 격리에 유의한다.

## 작업 방식
- 한 번에 다 만들지 말고, 단계별로 만들고 실행해 확인하며 진행할 것.
- 코드 작성 시마다 보안 / 동시접속 / 개발팀 전달 가능성을 함께 점검할 것.
- 파괴적 작업(파일 삭제 등) 전에는 반드시 확인받을 것.
