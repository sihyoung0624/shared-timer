# ==========================================================================
# 공유 타이머 - 필요 패키지 목록 (Python 3.11 기준)
# ==========================================================================
# 설치 방법:  pip install -r requirements.txt
#
# - 직접 설치한 것은 아래 Flask, Flask-SocketIO 2개뿐이며
#   나머지는 그 의존성으로 자동 설치됩니다.
# - 버전을 고정(==)해 둔 이유: 개발팀이 동일한 환경을 그대로 재현하기 위함.
#   (지금 동작이 검증된 조합입니다.)
# ==========================================================================

# --- 핵심 (직접 설치) ---
Flask==3.1.3
Flask-SocketIO==5.6.1
qrcode==8.2          # 뷰어 링크 QR 코드 생성 (SVG, PIL 불필요)

# --- 의존성 (자동 설치됨) ---
bidict==0.23.1
blinker==1.9.0
click==8.4.2
colorama==0.4.6
h11==0.16.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
python-engineio==4.13.3
python-socketio==5.16.3
simple-websocket==1.1.0
Werkzeug==3.1.8
wsproto==1.3.2

# --- 배포(인터넷 공개)용 운영 서버 ---
# 로컬 Windows 개발에는 불필요(python app.py 로 충분).
# 리눅스 호스팅(Render 등)에서 실행:  gunicorn -k eventlet -w 1 --bind 0.0.0.0:$PORT app:app
# [중요] gunicorn 과 eventlet 은 '서로 호환되는' 버전으로 고정해야 함.
#   최신끼리는 eventlet 작업자를 못 찾는 충돌 사례가 있어 검증된 조합으로 고정.
gunicorn==21.2.0
eventlet==0.33.3
