# 가상환경 (requirements.txt 로 재생성하므로 저장소에 올리지 않음)
.venv/
venv/

# Python 캐시
__pycache__/
*.pyc

# 환경변수 파일 (SECRET_KEY 등 비밀값 — 절대 커밋 금지)
.env

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로컬 도구 설정 (배포와 무관)
.claude/
