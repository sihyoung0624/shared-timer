# 인터넷 배포 가이드 (공유 타이머)

이 문서는 같은 와이파이용 MVP를 **인터넷에 공개**해서, 서로 다른 망(다른 와이파이·LTE/5G)에 있는
불특정 다수가 접속할 수 있게 만드는 방법입니다.

> ⚠️ **먼저 읽어주세요 — 꼭 알아야 할 3가지**
> 1. **데이터는 메모리에만 저장**됩니다. 서버가 재시작되면 진행 중이던 타이머가 모두 사라집니다.
> 2. 그래서 **반드시 워커 1개(`-w 1`)로만** 운영해야 합니다. 워커가 여러 개면 방 데이터가
>    서로 공유되지 않아 깨집니다. (대규모로 키우려면 맨 아래 "한계와 다음 단계" 참고)
> 3. 인터넷에 공개하면 **누구나 접속·방 생성**이 가능합니다. 코드에 기본 방어
>    (방 개수 상한·자동 정리)는 넣었지만, 민감 용도라면 추가 인증을 권장합니다.

---

## 방법 A. Render.com 에 올리기 (권장, 무료 가능)

가장 쉽고 무료로 시작할 수 있는 방법입니다. WebSocket(실시간)도 지원합니다.

### 준비물
- GitHub 계정 (코드를 올려둘 곳)
- Render 계정 (https://render.com — GitHub로 가입 가능)

### 단계
1. **코드를 GitHub 저장소에 올립니다.** (이 폴더 전체. `.venv`는 `.gitignore`로 자동 제외됨)
2. Render 로그인 → **New + → Blueprint** 선택
3. 방금 올린 GitHub 저장소를 연결합니다.
   → 저장소에 있는 `render.yaml`을 Render가 자동으로 읽어 설정합니다.
4. **Apply** 클릭 → 빌드가 시작됩니다. (수 분 소요)
5. 끝나면 `https://shared-timer.onrender.com` 같은 **공개 주소**가 나옵니다.
   이 주소를 사람들에게 공유하면, 어느 망에서든 접속됩니다. QR도 이 주소로 자동 생성됩니다.

### Render 대시보드에서 직접 설정하는 경우 (Blueprint 대신)
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `gunicorn -k eventlet -w 1 app:app`
- **Environment Variables:** 아래 표 참고

---

## 방법 B. 지금 당장 잠깐만 외부에 보여주기 (개발/데모용 터널)

서버를 진짜로 배포하지 않고, **지금 이 PC의 서버를 잠깐 인터넷에 노출**만 하는 방법입니다.
PC를 끄면 사라지고 보안이 약하므로 **데모·테스트 전용**입니다.

```bash
# 예: cloudflared 터널 (설치 후)
cloudflared tunnel --url http://localhost:5050
```
실행하면 임시 공개 주소(예: https://xxxx.trycloudflare.com)가 나옵니다.
이 주소로 접속하면 QR·링크도 그 공개 주소로 자동 생성됩니다.

---

## 배포 시 설정할 환경변수

| 변수 | 필수? | 설명 | 예시 |
|------|-------|------|------|
| `SECRET_KEY` | ✅ **필수** | 세션 서명용 비밀키. 무작위 긴 문자열. (Render Blueprint는 자동 생성) | `aF3x...(긴 무작위)` |
| `CORS_ORIGINS` | 권장 | 접속 허용 도메인. 안 주면 전체 허용(`*`). 보안상 본인 도메인으로 좁히길 권장 | `https://shared-timer.onrender.com` |
| `PUBLIC_BASE_URL` | 선택 | 링크·QR에 쓸 주소를 강제 지정. 보통 안 줘도 자동 인식됨 | `https://shared-timer.onrender.com` |
| `PORT` | 자동 | 호스팅이 자동 주입. 직접 설정 불필요 | (자동) |
| `MAX_ROOMS` | 선택 | 동시 존재 가능한 방 수 상한 (기본 500) | `1000` |
| `ROOM_TTL` | 선택 | 방 자동 삭제까지의 시간(초) (기본 21600=6시간) | `10800` |

---

## 배포 후 확인 체크리스트

- [ ] 공개 주소로 접속해 홈 화면이 뜨는가
- [ ] 타이머를 만들고 **다른 망(LTE)** 폰에서 QR로 접속되는가
- [ ] 주소가 `https://` 인가 (자물쇠 표시) — 토큰이 URL에 있으므로 HTTPS 필수
- [ ] 컨트롤러 링크로만 제어되고, 뷰어 링크로는 제어가 안 되는가
- [ ] `SECRET_KEY`가 코드 기본값이 아니라 환경변수로 설정됐는가

---

## 한계와 다음 단계 (개발팀 참고)

현재 구조는 **단일 서버 · 메모리 저장**이라 다음 한계가 있습니다.

- **수평 확장 불가:** 서버를 2대 이상으로 늘리면 방 데이터가 공유되지 않습니다.
  → 대규모(수백~수천 동시)로 키우려면 **Redis** 등 외부 저장소 + Socket.IO 메시지 큐 도입 필요.
- **영속성 없음:** 재시작 시 진행 중 타이머 소멸. 기록이 필요하면 DB 연동 필요.
- **남용 방지 강화 여지:** 현재는 방 개수 상한·자동 정리 수준. 공개 규모가 커지면
  IP별 생성 제한(rate limit), 캡차, 로그인 등 추가 권장.
- **무료 티어 슬립:** Render 무료 플랜은 미사용 시 잠들어, 첫 접속이 느립니다.
  실제 행사용이라면 유료 플랜(상시 가동)을 권장합니다.

> 이 모든 게 부담이라면, 상용 서비스(stagetimer.io 등)를 쓰는 것도 합리적인 선택입니다.
> 직접 운영은 "자체 브랜딩·기존 시스템 통합·데이터 통제"가 필요할 때 의미가 큽니다.
